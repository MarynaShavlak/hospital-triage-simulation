# Фази 4–5. Запуск, метрики та головний результат

> Розділ 06 · [↑ Зміст документації](README.md) · [↑ Головний README](../README.md)

[← Варіанти структури черги](05b-queue-variants.md)    [Фаза 6. Візуалізація (Графіки 1–4) →](07-visualizations.md)

---

## Фаза 4: Запуск усіх трьох сценаріїв

`deepcopy` — щоб кожна симуляція стартувала з "чистими" пацієнтами (не успадковувала `start_time` з попередньої).

```python
fifo_result   = simulate(copy.deepcopy(patients), 'fifo')
prio_result   = simulate(copy.deepcopy(patients), 'priority')
aging_result  = simulate_aging(copy.deepcopy(patients), aging_threshold=45)

print('Усі три сценарії відпрацювали:')
print(f'  FIFO:     обслужено {len(fifo_result)} пацієнтів')
print(f'  Priority: обслужено {len(prio_result)} пацієнтів')
print(f'  Aging:    обслужено {len(aging_result)} пацієнтів')
```


**Результат:**

```
Усі три сценарії відпрацювали:
  FIFO:     обслужено 180 пацієнтів
  Priority: обслужено 180 пацієнтів
  Aging:    обслужено 180 пацієнтів
```

## Фаза 5: Метрики та аналіз

Попередні фази **запустили** симуляцію — кожен пацієнт отримав `start_time` (коли його почали лікувати). Фаза 5 **перетворює сирі дані на зрозумілі цифри**: рахує середнє й максимальне очікування для кожного рівня тяжкості й виводить порівняльні таблиці.

Рахуємо середній і максимальний час очікування для кожного рівня тяжкості.

### Функція `metrics_by_severity`

```python
def metrics_by_severity(served):
    buckets = {severity: [] for severity in range(1, 6)}
    for patient in served:
        buckets[patient.severity].append(patient.wait_time)
    return {severity: {'avg': statistics.mean(waits), 'max': max(waits), 'count': len(waits)}
            for severity, waits in buckets.items() if waits}
```

Працює у два кроки.

**Крок 1 — розкладає по "відрах" (buckets).** Створює 5 порожніх списків (по одному на рівень) і кидає час очікування кожного пацієнта у відро його рівня:
```
buckets = {
  1: [2.0, 11.8, 7.4, 6.7, 14.8, 1.6],   ← всі L1
  2: [...],                                ← всі L2
  ...
}
```

**Крок 2 — рахує статистику для кожного відра.** Повертає словник із середнім, максимумом і кількістю:
```
{
  1: {'avg': 7.4, 'max': 14.8, 'count': 6},
  2: {'avg': 14,  'max': 69,   'count': 38},
  ...
}
```

Тобто функція бере список обслужених пацієнтів і повертає **готову статистику по рівнях**.

Подивимось на реальну структуру 👇

```python
# Що насправді повертає metrics_by_severity (на прикладі FIFO)

metrics_fifo = metrics_by_severity(fifo_result)

print("Структура metrics_fifo (метрики FIFO):\n")
for severity in range(1, 6):
    print(f"  Рівень {severity}: {metrics_fifo[severity]}")

print("\nЯк дістати конкретне число:")
print(f"  metrics_fifo[1]['avg']   = {metrics_fifo[1]['avg']:.1f}  (середнє очікування критичних)")
print(f"  metrics_fifo[1]['max']   = {metrics_fifo[1]['max']:.1f}  (максимум для критичних)")
print(f"  metrics_fifo[1]['count'] = {metrics_fifo[1]['count']}     (скільки було критичних)")
```


**Результат:**

```
Структура metrics_fifo (метрики FIFO):

  Рівень 1: {'avg': 79.81884183210724, 'max': 115.7260088790257, 'count': 6}
  Рівень 2: {'avg': 52.75952135550885, 'max': 138.6798875085882, 'count': 37}
  Рівень 3: {'avg': 62.47764815298807, 'max': 151.91688721395928, 'count': 56}
  Рівень 4: {'avg': 58.74600324431592, 'max': 158.6355118081001, 'count': 57}
  Рівень 5: {'avg': 67.63926508479051, 'max': 162.99474666748415, 'count': 24}

Як дістати конкретне число:
  metrics_fifo[1]['avg']   = 79.8  (середнє очікування критичних)
  metrics_fifo[1]['max']   = 115.7  (максимум для критичних)
  metrics_fifo[1]['count'] = 6     (скільки було критичних)
```

```python
metrics_fifo = metrics_by_severity(fifo_result)
metrics_priority = metrics_by_severity(prio_result)
metrics_aging = metrics_by_severity(aging_result)

print('СЕРЕДНІЙ час очікування (хв) за тяжкістю:\n')
print(f"{'Рівень':<18}{'FIFO':>8}{'Priority':>10}{'Aging':>8}")
print('-' * 44)
for severity in range(1, 6):
    print(f"{SEV_NAMES[severity]:<18}{metrics_fifo[severity]['avg']:>8.0f}{metrics_priority[severity]['avg']:>10.0f}{metrics_aging[severity]['avg']:>8.0f}")

print('\nМАКСИМАЛЬНИЙ час очікування (хв) за тяжкістю:\n')
print(f"{'Рівень':<18}{'FIFO':>8}{'Priority':>10}{'Aging':>8}")
print('-' * 44)
for severity in range(1, 6):
    print(f"{SEV_NAMES[severity]:<18}{metrics_fifo[severity]['max']:>8.0f}{metrics_priority[severity]['max']:>10.0f}{metrics_aging[severity]['max']:>8.0f}")
```


**Результат:**

```
СЕРЕДНІЙ час очікування (хв) за тяжкістю:

Рівень                FIFO  Priority   Aging
--------------------------------------------
L1 Критичний            80         7      17
L2 Невідкладний         53        14      33
L3 Терміновий           62        40      67
L4 Менш терм.           59       124      88
L5 Нетерміновий         68       303     138

МАКСИМАЛЬНИЙ час очікування (хв) за тяжкістю:

Рівень                FIFO  Priority   Aging
--------------------------------------------
L1 Критичний           116        15      34
L2 Невідкладний        139        69      96
L3 Терміновий          152       191     147
L4 Менш терм.          159       351     201
L5 Нетерміновий        163       703     246
```

### Три обчислення

Викликаємо функцію тричі — для кожного сценарію. Тепер маємо три словники зі статистикою. Доступ до конкретного числа читається ланцюжком:

```
metrics_fifo[1]['avg']   → metrics_fifo (таблиця FIFO) → [1] (рівень 1) → ['avg'] (середнє) = 80
metrics_priority[1]['avg']   → те саме під Priority = 7
metrics_aging[5]['max']   → максимум для легких під Aging = 246
```

```python
# Головна цифра дослідження
print('=' * 50)
print('ГОЛОВНИЙ РЕЗУЛЬТАТ')
print('=' * 50)
print(f"Критичний пацієнт (L1) у середньому чекає:")
print(f"   FIFO:     {metrics_fifo[1]['avg']:.0f} хвилин")
print(f"   Priority: {metrics_priority[1]['avg']:.0f} хвилин")
print(f"   → у {metrics_fifo[1]['avg'] / metrics_priority[1]['avg']:.0f} рази швидше з чергою з пріоритетами!\n")
print(f"Найдовше очікування критичного пацієнта:")
print(f"   FIFO:     {metrics_fifo[1]['max']:.0f} хвилин (це понад {metrics_fifo[1]['max']/60:.1f} години!)")
print(f"   Priority: {metrics_priority[1]['max']:.0f} хвилин")
```


**Результат:**

```
==================================================
ГОЛОВНИЙ РЕЗУЛЬТАТ
==================================================
Критичний пацієнт (L1) у середньому чекає:
   FIFO:     80 хвилин
   Priority: 7 хвилин
   → у 11 рази швидше з чергою з пріоритетами!

Найдовше очікування критичного пацієнта:
   FIFO:     116 хвилин (це понад 1.9 години!)
   Priority: 15 хвилин
```

### Звідки взялися «11×» і «майже 2 години»

Витягуємо головну цифру дослідження — порівняння критичних (L1):
- `mf[1]['avg']` = 80 (FIFO)
- `mp[1]['avg']` = 7 (Priority)
- `mf[1]['avg'] / mp[1]['avg']` = 80 / 7 ≈ 11.4 → округлено `:.0f` → **11**

Звідси "у 11 разів швидше" — це проста арифметика: у скільки разів 80 більше за 7.

Далі максимум із перетворенням у години:
```python
print(f"   FIFO: {metrics_fifo[1]['max']:.0f} хвилин (понад {metrics_fifo[1]['max']/60:.1f} години!)")
```
- `mf[1]['max']` = 116 хв
- `mf[1]['max']/60` = 116/60 ≈ 1.9 години

Ділення на 60 перетворює хвилини на години — щоб "116 хвилин" звучало драматичніше як "майже 2 години".

### Як ЧИТАТИ таблиці (найважливіше)

Подивіться на перший рядок таблиці середніх:
```
L1 Критичний          80         7      17
                    FIFO   Priority  Aging
```

**Читаємо як історію зліва направо:**
- **FIFO = 80:** критичний чекає 80 хв, застряг за чергою легших. Погано.
- **Priority = 7:** той самий критичний чекає 7 хв, його пропустили вперед. Чудово.
- **Aging = 17:** компроміс — трохи повільніше за priority, але набагато краще за FIFO.

Останній рядок (легкі L5):
```
L5 Нетерміновий       68       303     138
```
- **FIFO = 68:** легкі чекають помірно.
- **Priority = 303:** легкі чекають утричі довше — зворотний бік priority (рятує критичних коштом легких).
- **Aging = 138:** компроміс повертає легких до прийнятного рівня.

**Ключовий патерн обох таблиць:**
- під **Priority** числа **ростуть** зверху вниз (L1→L5): критичним краще, легким гірше;
- під **FIFO** числа приблизно **однакові** — усім байдуже однаково (несправедливо до критичних);
- **Aging** — посередині, згладжує крайнощі.

Таблиця максимумів показує те саме, але гостріше: під Priority максимум L5 = **703 хв** (пацієнт #82, що чекав 12 годин), а Aging зрізає це до 246.


---

[← Варіанти структури черги](05b-queue-variants.md)    [Фаза 6. Візуалізація (Графіки 1–4) →](07-visualizations.md)
